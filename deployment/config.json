{
  "model_config_list": [
    {
      "config": {
        "name": "text-classifier",
        "base_path": "/models/text-classifier",
        "target_device": "CPU",
        "batch_size": "1"
      }
    }
  ]
}
